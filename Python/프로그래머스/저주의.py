# 3의 배수와 숫자 3 사용 x
def solution(n):
    # a = n%3
    # b = n//3
    # c = b * 3 + b + a
    # if c % 3 == 0 or "3" in str(c):
    #     return c + b
    # return c 
    while   
        
print(solution(15))